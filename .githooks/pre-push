#!/bin/sh
# Tests run before every push. Installed by `npm install` via the prepare
# script (git config core.hooksPath .githooks). Skip once with --no-verify.
echo "pre-push: running npm test"
npm test
